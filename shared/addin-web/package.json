{
  "name": "@docvault/addin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared task-pane UI + bridge client for the DocVault Office/WPS add-ins",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/office-js": "^1.0.0",
    "typescript": "^5.5.0",
    "vite": "^8.1.4"
  }
}
