{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set: core IPC + event listen, and the file dialog plugin for commit/export/checkout.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
